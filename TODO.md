# TODO — قائمة مهام تطبيق ذكّر (Dhakkir)

تحديث: تم تنفيذ كل البنود الحرجة والعالية والمتوسطة من المراجعة الأصلية (راجع `CHANGELOG.md` للتفاصيل الكاملة). الباقي أدناه بنود تحتاج قرارًا من المالك أو بنية تحتية إضافية خارج نطاق تطبيق واجهة أمامية ثابت.

## ✅ تم إنجازه

- [x] إصلاح بنية مجلدات Create React App (`public/` + `src/`) — البناء ينجح الآن فعليًا (تم التحقق بـ `npm run build`).
- [x] إضافة `.gitignore` و`package-lock.json`.
- [x] إزالة ملف الأندرويد الثنائي `Dhakkir ذكّر .aab` من التتبع.
- [x] حفظ حالة المستخدم (العدّادات، الإنجاز، اللغة، إظهار الترجمة، التذكيرات) في `localStorage`.
- [x] توحيد ترجمة زر "رجوع" حسب اللغة المختارة.
- [x] تنفيذ تذكيرات أساسية باستخدام حقل `time` (انظر القيد أدناه).
- [x] إضافة `manifest.json` وأيقونات PWA أساسية.
- [x] إزالة المفاتيح المكررة (`translit`) من بيانات `ADHKAR`.
- [x] إزالة الكود الميت (كائن الأنماط `S` والتعريفات المكررة لـ `BG`/`GOLD`/`TEXT`/`CARD`).
- [x] تصحيح الأخطاء الإملائية/الترميز في نصوص الأحاديث الإنجليزية.
- [x] تقسيم `App.jsx` إلى `data/` و`hooks/` و`components/`.
- [x] إضافة اختبارات أساسية (`react-scripts test`) للعرض ولسلامة البيانات.
- [x] إضافة CI عبر GitHub Actions (`npm test` + `npm run build` على كل push/PR).
- [x] اتجاه الصفحة (`dir`) يتبدّل ديناميكيًا الآن حسب اللغة المختارة.
- [x] إضافة `aria-label` على الأزرار الأيقونية الرئيسية (الإعدادات، السابق/التالي، الضغط للعدّ).
- [x] إضافة `CHANGELOG.md`.
- [x] إضافة مواعيد الصلاة بالحساب الفلكي (Fajr–Isha) بناءً على الموقع الجغرافي، مع 5 طرق حساب ومذهبي عصر، واختبارات تحقق من صحة الفلك (ترتيب زمني، تناظر الاعتدال، طول اليوم عند خط الاستواء، مقارنة بقيم حقيقية معروفة لمكة). التذكيرات تشمل الآن مواعيد الصلاة أيضًا.
- [x] إصلاح تعليق طلب الموقع الجغرافي إلى الأبد بلا استجابة عند عدم رد المتصفح على إذن الموقع (حارس زمني/watchdog يضمن الخروج من حالة "جاري التحديد" خلال 20 ثانية كحد أقصى)، مع رسائل خطأ دقيقة تفرّق بين رفض الإذن/بطء GPS/تعذّر التحديد.
- [x] إضافة قسم **القرآن الكريم** كامل: مصحف بشكل صفحات حقيقية (604 صفحة مطابقة لمصحف مجمع الملك فهد)، فهرس بالسور، بحث نصي متسامح مع غياب التشكيل، حفظ آخر صفحة قُرئت. البيانات مبنية من مصادر مفتوحة (راجع "الترخيص والمصادر" في README) ومُحمَّلة كـ chunk منفصل (code-split) فلا تُبطئ باقي التطبيق.
- [x] تذكيرات اذكار الصباح/المساء/النوم/الاستيقاظ تتبع الآن مواقيت الفجر/الشروق/العصر/العشاء الحقيقية لموقعك الجغرافي (بدل أوقات ثابتة افتراضية)، مع رجوع تلقائي للأوقات الثابتة لو الموقع مش مفعّل.
- [x] شاشة البداية (Splash) بقت تعرض شكل النجمة (الوردة ذات الـ16 رأس المستخدمة أصلًا في أيقونة التطبيق) كتوهج خلفي خلف كلمة "ذكّر" مباشرة ومتمركز بالظبط في نصها، بدل أيقونة صغيرة منفصلة فوقها — مطابقة لشكل اللوجو الحقيقي.
- [x] المصحف: وضع ملء الشاشة (Fullscreen/Immersive) لتسهيل القراءة على كبار السن أو ضعاف النظر — يخفي كل عناصر الواجهة الأخرى ويسمح بتكبير حجم الخط، والتنقل بين الصفحات بالسحب (Swipe) يمينًا أو يسارًا بالإضافة لأزرار السابق/التالي.
- [x] البحث عن آية: الضغط على نتيجة بحث يفتح الآن عرضًا للآية بمفردها، مع تنقل للآية التالية/السابقة (بالأزرار أو بالسحب)، وزر لعرض نفس الآية داخل صفحة المصحف الفعلية مع تظليلها فيها.

## 🟡 متبقٍ — يحتاج قرارًا أو بنية إضافية

- [ ] **مواعيد الصلاة تفترض أن توقيت الجهاز مطابق لموقعه الجغرافي.** هذا صحيح تلقائيًا عند استخدام تحديد الموقع من نفس الجهاز، لكنه قد يخطئ إن كان توقيت النظام مضبوطًا يدويًا على منطقة زمنية مختلفة عن الموقع الفعلي (حالة نادرة). حل كامل يتطلب مكتبة بيانات مناطق زمنية جغرافية إضافية (حجم أكبر للحزمة) — لم يُنفَّذ لتفادي تضخيم التطبيق لحالة نادرة.
- [ ] **دقة مواعيد الصلاة تقريبية (±بضع دقائق) مقارنة بالجداول الرسمية المحلية**، لأن بعض الهيئات تضيف تعديلات يدوية محلية (ارتفاع الأفق، إجماع لجنة) فوق الحساب الفلكي البحت. هذا سلوك متوقع لأي حاسبة فلكية عامة، ويمكن تخفيفه مستقبلًا بإضافة إعداد "تعديل يدوي بالدقائق" لكل صلاة.

- [ ] **التذكيرات حاليًا محدودة بالمقدمة (foreground) فقط.** التنفيذ الحالي (`src/hooks/useReminders.js`) يستخدم `Notification` API عبر `setInterval` أثناء فتح التبويب فقط. تذكير حقيقي في الخلفية يتطلب Service Worker + اشتراك Push مع خادم فعلي — خارج نطاق تطبيق واجهة أمامية ثابت بلا Backend. إن كانت التذكيرات الخلفية مطلوبة فعليًا، يلزم تصميم بنية خلفية (مثل Firebase Cloud Messaging أو Web Push + خادم صغير).
- [ ] **مراجعة دقة نسبة الأحاديث والأذكار.** تم تصحيح الأخطاء الإملائية فقط في النصوص الإنجليزية؛ لم تُراجَع صحة العزو الحديثي (تخريج/تصحيح/تحسين) مقابل مرجع معتمد كحصن المسلم — يحتاج مراجعة من مختص أو مصدر موثوق ثم توثيقه في README.
- [ ] **إضافة ملف `LICENSE`.** قرار الترخيص (MIT؟ خاص؟) يعود لمالك المشروع.
- [ ] **أيقونات PWA الحالية بسيطة (شكل هندسي مولّد برمجيًا مطابق لعلامة التطبيق).** جيدة كبداية للتطوير، لكن يُفضّل تصميم أيقونة نهائية احترافية قبل نشر فعلي على متجر Google Play (خصوصًا عند إعادة بناء ملف `.aab`).
- [ ] تفعيل تدفق عمل Git طبيعي (فروع، مراجعات Pull Request) للتغييرات القادمة بدل الرفع المباشر.
- [ ] **مراجعة نص القرآن حرفيًا مقابل مصحف معتمد قبل نشر واسع.** تم التحقق برمجيًا من: تطابق عدد الآيات (6236)، تطابق تخطيط الصفحات مع تخطيط مجمع الملك فهد، عدم وجود آيات مفقودة أو مكررة، وتدقيق يدوي لعدة آيات معروفة (آية الكرسي، الإخلاص، أول وآخر آية في المصحف) — لكن لم تتم مراجعة حرفية كاملة (6236 آية) من قِبل مختص. يُنصح بذلك قبل الاعتماد الكامل على النص في سياق ديني.
- [ ] **البحث في القرآن قد لا يجد بعض الكلمات نادرة الاستخدام** التي يُكتب فيها الألف الخنجرية (ٰ) في الرسم العثماني بديلاً عن ألف كاملة موجودة في الإملاء الحديث (مثل بعض تصريفات "الملائكة")، لأن التطبيع الحالي يحذف الألف الخنجرية كتشكيل بدل تحويلها لألف — قرار عملي موثّق في `src/utils/arabic.js` لتفادي كسر البحث عن الكلمات الأكثر شيوعًا (كـ"الرحمن") التي تعمل بشكل صحيح مع النهج الحالي.
- [ ] **صفحات المصحف لا تطابق سطور الطباعة الأصلية حرفيًا** (فقط تطابق تجميع الآيات في نفس رقم الصفحة، وهو الأهم للتصفح والفهرسة) — التطابق الكامل سطرًا بسطر يتطلب خطوط QCF خاصة (عشرات الميجابايت) خارج نطاق هذا التطبيق الخفيف. النص الآن `justify` (وليس محاذاة يمين) عشان السطور توصل لطرفي الصفحة زي المصحف المطبوع — على متصفحات WebKit/Safari (iOS) ده بيظهر بتمدد الكشيدة (kashida) تلقائيًا زي التطبيقات الأصلية، لكن على Chrome/Android بيتمدد التباعد بين الكلمات بس (Blink مفيهوش دعم كشيدة) وده أقل أناقة على سطر فيه كلمتين/تلاتة بس — قيد معروف من نفس القيد الأساسي (يحتاج خطوط QCF لحل كامل).

# TODO — قائمة مهام تطبيق ذكّر (Dhakkir)

قائمة ناتجة عن مراجعة الكود الحالي، مرتّبة حسب الأولوية. كل بند تم التحقق منه فعليًا في الكود (وليس افتراضًا).

## 🔴 حرجة (تمنع تشغيل المشروع)

- [ ] **إصلاح بنية مجلدات Create React App.** `react-scripts` يتطلب `public/index.html` و`src/index.js`، بينما `index.html` و`index.js` و`App.jsx` موجودة في جذر المشروع. تم التحقق: تشغيل `npm run build` يفشل فورًا بالخطأ:
  ```
  Could not find a required file.
    Name: index.html
    Searched in: <root>/public
  ```
  **الإصلاح:** إنشاء `public/index.html` (نقل الملف الحالي) و`src/index.js` + `src/App.jsx` (نقل الملفين)، وتحديث المسار داخل `index.js` (`import App from './App'`) إن لزم.

- [ ] **إضافة `.gitignore`.** لا يوجد حاليًا، ما يعني أن `node_modules/` و`build/` و`package-lock.json` (إن أُنشئ محليًا) قد تُرفع بالخطأ. أضف على الأقل:
  ```
  node_modules/
  build/
  .DS_Store
  ```

- [ ] **إزالة ملف الأندرويد الثنائي من الجذر.** `Dhakkir ذكّر .aab` (3.2MB, Android App Bundle مبني) لا ينبغي تتبعه داخل مستودع الكود المصدري. انقله إلى GitHub Releases، أو Git LFS، أو احذفه إن لم يعد يمثل أحدث نسخة.

- [ ] **توليد والتزام `package-lock.json`.** غير موجود حاليًا، ما يمنع تثبيتًا متطابقًا (reproducible install) بين البيئات وCI.

## 🟠 عالية

- [ ] **لا يوجد أي حفظ لحالة المستخدم.** عدّاد كل ذكر (`counts`)، حالة "تم الإنجاز" (`done`)، اللغة المختارة (`lang`)، وخيار إظهار الترجمة (`showTr`) كلها `useState` بلا أي `localStorage`. **أي تحديث للصفحة يصفّر كل التقدم.** يجب تحميلها من `localStorage` عند الإقلاع وحفظها عند كل تغيير.

- [ ] **زر "رجوع" ثابت بالعربية بغض النظر عن اللغة.** في شاشتي عرض الذكر (`App.jsx` صفحة `view==="dhikr"`) والتصنيف (`view==="category"`) النص مكتوب حرفيًا `"< رجوع"` حتى عند اختيار `en`/`de`، بينما بقية الواجهة مترجمة بالكامل عبر دالة `T()`. عدم اتساق ملحوظ في التجربة.

- [ ] **حقل `time` في `CATEGORIES` غير مستخدم إطلاقًا.** كل تصنيف أذكار (صباح 05:00، مساء 17:00، نوم 21:00، استيقاظ 06:00) يحمل وقتًا مقترحًا، لكن لا يوجد أي كود يقرأ هذا الحقل — لا إشعارات، لا تذكيرات، لا جدولة. إما تنفيذ تذكيرات فعلية (Notification API + Service Worker) بالاعتماد على هذا الحقل، أو حذفه إن لم يكن ضمن الخطة.

- [ ] **لا يوجد PWA (manifest.json / Service Worker).** لا آيقونات، لا `manifest.json`، لا عمل دون اتصال — رغم أن وجود ملف `.aab` يوحي بأن نية المشروع تحويله إلى تطبيق أندرويد (غالبًا عبر TWA/Bubblewrap الذي يتطلب PWA صالح ومستضاف فعليًا). يجب إضافة `public/manifest.json` وأيقونات وService Worker أساسي.

## 🟡 متوسطة

- [ ] **مفاتيح مكرّرة في كائنات JS ضمن بيانات الأذكار (`ADHKAR`).** مثال (سطر ضمن تصنيف `morning`):
  ```js
  {arabic:"...", translit:"Subhanallah", translit:"Subhanallahi wa bihamdih", ...}
  ```
  وكذلك في تصنيف `quran`. المفتاح الأول لـ `translit` يُتجاهل بصمت من محرك JavaScript دون أي خطأ أو تحذير — يجب حذف التكرار من كل الملف (`ADHKAR`).

- [ ] **تعريف مكرر لمتغيرات الألوان (`BG`, `GOLD`, `TEXT`, `CARD`) وكائن أنماط `S` بالكامل غير مستخدم.** المتغيرات معرّفة مرتين بـ `var` (مرة قرب كائن `S`، ومرة أخرى بعده مباشرة)، وكائن `S` (أنماط جاهزة: `app`, `hdr`, `ttl`, ...) غير مُستخدم في أي مكان بالمكوّن — كل التنسيق مكتوب inline يدويًا. حذف الكود الميت (`S` وأحد التعريفين المكررين).

- [ ] **أخطاء إملائية/ترميز متكررة في النصوص الإنجليزية للأحاديث.** حرف "ß" الألماني تسرّب إلى كلمات إنجليزية عبر ملف `HADITHS`، مثل: `tongü` (بدل tongue)، `Richneß`/`richneß` (Richness)، `truthfulneß` (truthfulness)، `righteousneß` (righteousness)، `forgiveneß` (forgiveness). يلزم مراجعة نصوص `en` كاملة في `HADITHS` وتصحيحها.

- [ ] **`App.jsx` ضخم (~1300 سطر) يجمع البيانات والمكوّن في ملف واحد.** يُفضّل:
  - فصل البيانات (`ADHKAR`, `HADITHS`, `CATEGORIES`, `HADITH_CATS`) إلى ملفات مستقلة (`src/data/adhkar.js`, `src/data/hadiths.js`).
  - تقسيم المكوّن الواحد إلى مكوّنات فرعية: `Splash`, `Settings`, `DhikrView`, `CategoryView`, `HadithView`, `HomeView`.

- [ ] **لا توجد اختبارات إطلاقًا** رغم أن `react-scripts` يهيّئ Jest جاهزًا للاستخدام دون إعداد إضافي.

- [ ] **لا يوجد CI.** إضافة GitHub Actions بسيط (`npm ci && npm run build`) كان سيكشف مشكلة البنية الحرجة (البند الأول) فور أول Pull Request.

## 🟢 منخفضة / تحسينات مستقبلية

- [ ] توثيق مصدر نصوص الأذكار/الأحاديث (كحصن المسلم أو مرجع حديثي معتمد) في README للتحقق من صحة النسبة والتخريج.
- [ ] اتجاه الصفحة (`dir`) ثابت على `rtl` في `index.html` بغض النظر عن اللغة المختارة (`en`/`de`) — يُفضّل تبديله ديناميكيًا حسب `lang`.
- [ ] تحسين إمكانية الوصول (accessibility): لا `aria-label` على الأزرار الأيقونية (⚙ الإعدادات، `<`/`>` التنقل)، ولا عناصر HTML دلالية (كل شيء `div`/`button`).
- [ ] إضافة `CHANGELOG.md` وملف `LICENSE`.
- [ ] الالتزامات الحالية في السجل (`git log`) كلاهما "Add files via upload" — يُفضّل تفعيل تدفق عمل Git طبيعي (فروع، رسائل التزام وصفية) للتغييرات القادمة.
